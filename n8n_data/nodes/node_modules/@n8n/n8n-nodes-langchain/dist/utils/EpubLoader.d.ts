/// <reference types="node" />
import { parseEpub } from '@gxl/epub-parser';
import { BaseDocumentLoader } from 'langchain/document_loaders/base';
import { Document } from 'langchain/document';
export declare class N8nEPubLoader extends BaseDocumentLoader {
    file: Buffer;
    private splitChapters;
    constructor(file: Buffer, { splitChapters }?: {
        splitChapters?: boolean | undefined;
    });
    protected parse(epub: ReturnType<typeof parseEpub>): Promise<Array<{
        pageContent: string;
        metadata?: object;
    }>>;
    load(): Promise<Document[]>;
}
